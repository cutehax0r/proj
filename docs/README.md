# documentation

These are subject to change.

* See [example config](./example_config.yml) for an example configuration file

* See [example html](./example_html/) for an example HTML project template

# Variable Parsing Test

This ensures that variables passed as command line arguments are properly required. It also ensures
that if a variable is required it can be generated by scripts and passed along.

All command line variables are passed as strings, scripts / templates should anticipate that if it
matters.

* can pass single variable
* can pass multiple variables
* variables can be defined by scripts
* missing required variables cause generation to fail

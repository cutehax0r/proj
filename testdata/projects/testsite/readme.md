# This is a test project

It's used to make sure that the "add" functionality works as expected.
We'll copy this to a temp directory then use `add` and make sure that
the expected changes are copied.

